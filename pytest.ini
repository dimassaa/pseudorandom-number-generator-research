[pytest]
# Inject the repo root into sys.path so the `src` package is importable under a
# bare `pytest` invocation, not only `python3 -m pytest`. CI and later stages
# must not depend on CWD being implicitly added by the -m form.
pythonpath = .
testpaths = tests